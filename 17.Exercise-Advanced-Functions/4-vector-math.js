function solve() {
  function add(x, y) {}

  function multiplay(x, y) {}

  function length(x) {}

  function dot(x, y) {}

  function cross(x, y) {}

  return {
    add,
    multiplay,
    length,
    dot,
    cross,
  };
}

const vectorMath = solve();